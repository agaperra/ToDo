<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ToDo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purpleMainDark</item>
        <item name="colorPrimaryVariant">@color/purpleMainDark</item>
        <item name="colorOnPrimary">@color/purpleMainDark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/purpleMainDark</item>
        <item name="colorSecondaryVariant">@color/purpleMainDark</item>
        <item name="colorOnSecondary">@color/purpleMainDark</item>
        <item name="android:windowBackground">@color/purpleMainDark</item>
        <item name="android:textColor">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>

        <item name="android:navigationBarColor">?android:attr/windowBackground</item>
        <!-- Customize your theme here. -->
    </style>
</resources>